# ============================================
# Network Engineers Toolkit - Production Environment
# ============================================
# Copy this file to .env.prod and configure with your values
# DO NOT commit .env.prod to git (it's in .gitignore)

# Database Configuration
# Note: Using PostgreSQL in Docker (not Hostinger's MySQL)
DB_HOST=db
DB_PORT=5432
DB_USER=nettools_user
DB_PASSWORD=CHANGE_ME_SECURE_PASSWORD_HERE_MIN_16_CHARS
DB_NAME=nettools_prod

# Security Keys
# Generate with: openssl rand -base64 32
# ENCRYPTION_KEY must be exactly 32 characters
ENCRYPTION_KEY=CHANGE_ME_32_BYTE_KEY_HERE_32_CHAR
JWT_SECRET=CHANGE_ME_LONG_RANDOM_SECRET_HERE_MIN_64_CHARS

# Email Configuration (Optional - for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
FROM_EMAIL=noreply@your-domain.com

# Application Configuration
NODE_ENV=production
PORT=3000
APP_URL=https://your-domain.com

# ============================================
# SECURITY NOTES:
# ============================================
# 1. Replace ALL "CHANGE_ME" values before deployment
# 2. Generate secure random keys using:
#    openssl rand -base64 32 | head -c 32  # For ENCRYPTION_KEY
#    openssl rand -base64 64               # For JWT_SECRET
# 3. Keep .env.prod file secure - never commit to git
# 4. Ensure ENCRYPTION_KEY is exactly 32 characters
# 5. Use strong passwords (minimum 16 characters)
# 6. Change admin password after first login
# ============================================

# Quick Setup Commands:
# ---------------------
# 1. Copy this file:
#    cp .env.prod.example .env.prod
#
# 2. Generate ENCRYPTION_KEY (must be 32 chars):
#    openssl rand -base64 32 | head -c 32
#
# 3. Generate JWT_SECRET:
#    openssl rand -base64 64
#
# 4. Edit .env.prod and replace all CHANGE_ME values
#
# 5. Verify no placeholders remain:
#    grep CHANGE_ME .env.prod
#    (should return nothing)